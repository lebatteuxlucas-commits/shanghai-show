{
  "name": "shanghai-show",
  "private": true,
  "type": "module",
  "description": "FutureMotion — China Cycle 2026 Supplier Directory (HTML statique + route /api/catalogue-request)",
  "scripts": {
    "import:catalogues": "node scripts/import-catalogue-matches.ts",
    "db:migrate": "node --env-file=.env.local scripts/db-migrate.mjs",
    "test": "node --test tests/*.test.ts",
    "review": "REVIEW_MODE=1 node scripts/review-server.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  }
}
